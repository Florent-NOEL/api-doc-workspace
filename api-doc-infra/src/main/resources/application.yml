spring:
  application:
    name: api-doc-hexagonal
  datasource:
    url: jdbc:postgresql://localhost:5432/documents_db
    username: ${app_user}
    password: ${app_password}

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true